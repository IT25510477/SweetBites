package com.bakery.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Database configuration handled via application.properties
    // All data access is performed through Spring Data JPA Repository interfaces
}
